/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "http://www.w3.org/2000/svg",
      "assets.dummyjson.com",
      "cdn.dummyjson.com",
    ],
  },
};

export default nextConfig;
